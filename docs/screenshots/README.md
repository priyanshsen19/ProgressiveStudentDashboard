# Screenshots

Capture these from a running instance (`npm run dev`, then log in as `student@example.com` /
`Password123!`) and save them here so the README can reference them:

| File                    | What to capture                                              |
| ----------------------- | ----------------------------------------------------------- |
| `01-login.png`          | The login page                                              |
| `02-dashboard.png`      | Student dashboard (KPIs + trend + donut + courses + recs)   |
| `03-course.png`         | A course detail page with its lesson list                   |
| `04-lesson.png`         | A lesson page showing the "Mark Lesson Complete" action     |
| `05-swagger.png`        | `http://localhost:4000/api/docs`                            |

All screenshots should show real, seeded application data — not placeholder content.
